
  # Fifty Percent Crarbon

  This is a code bundle for Fifty Percent Crarbon. The original project is available at https://www.figma.com/design/T3DTtfiUw8eyLT68ysvbdf/Fifty-Percent-Crarbon.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  